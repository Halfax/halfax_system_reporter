psutil==6.0.0
py-cpuinfo==9.0.0

# Windows-specific (optional, gracefully handled on Linux/Pi)
wmi==1.5.1; sys_platform == 'win32'
pywin32==306; sys_platform == 'win32'

# Note: tkinter is included with Python by default
# On Linux/Ubuntu: sudo apt-get install python3-tk
# On Raspberry Pi: sudo apt-get install python3-tk
